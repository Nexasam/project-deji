<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $property->name }} – Admin review</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-50 font-sans text-slate-950 antialiased"><main class="mx-auto max-w-5xl px-4 py-6 lg:px-8"><a href="{{ route('admin.properties.index') }}" class="text-sm font-bold text-orange-600">← Property publishing</a>
@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif
@if($errors->any())<div class="mt-5 rounded-xl bg-red-50 p-4 text-sm font-bold text-red-800">{{ $errors->first() }}</div>@endif
<section class="mt-5 rounded-2xl border border-slate-200 bg-white p-6"><div class="flex flex-col justify-between gap-5 sm:flex-row"><div><p class="text-xs font-bold uppercase tracking-wider text-orange-600">{{ $property->business->name }}</p><h1 class="mt-2 text-2xl font-extrabold">{{ $property->name }}</h1><p class="mt-2 text-sm text-slate-500">{{ $property->code }} · {{ str($property->publication_status->value)->title() }} · {{ str($property->verification_status->value)->title() }}</p></div><div class="flex items-start gap-2">@if($property->publication_status->value === 'published')<form method="POST" action="{{ route('admin.properties.unpublish',$property) }}">@csrf<button class="rounded-lg border border-red-200 bg-red-50 px-4 py-2.5 text-sm font-bold text-red-700">Unpublish</button></form>@else<form method="POST" action="{{ route('admin.properties.publish',$property) }}">@csrf<button class="rounded-lg bg-orange-600 px-4 py-2.5 text-sm font-bold text-white">Publish property</button></form>@endif</div></div>
<dl class="mt-8 grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-slate-500">Marketplace title</dt><dd class="font-bold">{{ $property->marketplaceListing?->public_title ?? 'Missing' }}</dd></div><div><dt class="text-slate-500">Marketplace slug</dt><dd class="font-bold">{{ $property->marketplaceListing?->slug ?? 'Missing' }}</dd></div><div><dt class="text-slate-500">Type</dt><dd class="font-bold">{{ str($property->property_type)->replace('_',' ')->title() }}</dd></div><div><dt class="text-slate-500">Booking mode</dt><dd class="font-bold">{{ str($property->booking_mode ?? 'not set')->replace('_',' ')->title() }}</dd></div><div><dt class="text-slate-500">Capacity</dt><dd class="font-bold">{{ $property->capacity }} guests</dd></div><div><dt class="text-slate-500">Nightly price</dt><dd class="font-bold">{{ $property->pricing_currency }} {{ number_format((float)$property->default_nightly_price) }}</dd></div><div><dt class="text-slate-500">Media</dt><dd class="font-bold">{{ $property->media->count() }} file(s)</dd></div><div><dt class="text-slate-500">Marketplace eligibility</dt><dd class="font-bold">{{ $property->marketplaceListing?->is_publication_eligible ? 'Eligible' : 'Not yet eligible' }}</dd></div></dl>
</section></main></body></html>
